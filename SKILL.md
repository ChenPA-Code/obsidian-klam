---
name: obsidian-klam
description: 整理对话中具有长期价值的内容并写入 Obsidian 笔记库。触发场景：用户说「整理这轮对话 / 沉淀到 Obsidian / 记到 Obsidian / 记录要点 / 保存结论 / 写入笔记库 / 存档 / 留档 / 归纳一下 / 把有用的记下来 / 做个笔记 / 知识沉淀 / 记笔记 / 整理聊天记录 / 把对话存下来」等。按项目归档，预览确认后直接以笔记形式写入对应项目目录。
---

# Obsidian-klam

把对话中「值得留档」的内容整理成结构化 Obsidian 笔记，按项目归档。

## 何时使用

- 用户说「整理这轮对话 / 沉淀到 Obsidian / 记到 Obsidian / 记录要点 / 保存结论 / 写入笔记库 / 存档 / 留档 / 归纳一下 / 把有用的记下来 / 做个笔记 / 知识沉淀 / 记笔记 / 整理聊天记录 / 把对话存下来」等表达；
- 会话中出现了需要留档的内容（决定、行动项、学习收获、资料、灵感、产出物等），用户希望写入笔记库。

## 前置条件

- vault 路径已配置：见 `scripts/config.sh`（环境变量 `VAULT_PATH` / 零配置探测 / `init.sh`）。
- 写入模式为**交互预览确认**：第 5 步未经用户确认，不得写入任何文件。

## 工作流

1. **确认项目名**：用户明确指定 → 否则从对话上下文推断 → 否则用 `inbox`。
2. **扫描会话**：读取本轮全部用户消息与系统/工具消息。
3. **去噪**：按 `references/extract-rules.md` 丢弃寒暄、重复、过程性噪音，识别对话主题。
4. **提取内容（自适应结构）**：先判断这段对话的性质（学习 / 工作整理 / 资料收集 / 聊天要点 / 混合），再按内容自然组织小节。**不强制固定分类**，从模块库中按需组合：背景与事实、关键决定、行动项、学习要点、资料与链接、灵感与想法、待讨论问题、产出物、原话摘录。只保留「有长期价值」的内容。
5. **预览确认（人工门）**：向用户展示待写入清单（项目名 / 标题 / 内容摘要），确认后才继续。
6. **直接写入**：调用 `scripts/write_note.sh` 生成笔记，直接写入 `Projects/<项目名>/` 目录（`status: reviewed`）。
7. **更新项目索引**：按 `references/project-moc.md` 刷新对应 `Projects/<项目>/_index.md`，汇报结果。

## 幻觉缓解（强制）

AI 生成的笔记可能存在幻觉，以下机制必须执行：

- **原文引用**：每个条目标注来源——引用户原话片段或标注消息序号（如「用户原话：……」），便于人工对照。
- **宁缺毋滥**：对话中未出现、无把握的内容一律不写；不确定的数字标注「（待核实）」。
- **推断标注**：AI 推断的内容必须标注「（推断）」。
- **人工把关**：写入前经第 5 步预览确认；写入后建议用户抽查关键条目（尤其是数字、决定、行动项）。

## 输出规范

目录结构、properties、正文模块、命名规则：见 `references/note-schema.md`。

## 边界规则（强制）

- **敏感信息**：命中 token / API key / 密码 / 密钥 / 证件号 / 手机号 / 银行卡等，一律不写入；提示用户可脱敏后另存。
- **幂等**：写入前用 `scripts/check_duplicate.sh --source <会话ID>` 检测；同会话已整理过则询问「更新 / 跳过」。
- **失败处理**：写入失败时，整理结果完整保留在回复中，不静默丢弃。
- **超长会话**：先按主题分层摘要，再逐段提取；超限时输出「结论 + 行动项」精简版。
